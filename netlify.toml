[build]
  command = "echo Build complete"
  functions = "netlify/functions"
  publish = "."

[[redirects]]
  from = "/*"
  to = "/"
  status = 200

[functions]
  directory = "netlify/functions"
  node_bundler = "nft"
  included_files = ["templates/**"]

[functions.generate-report]
  timeout = 26

[context.production.environment]
  # Secrets (MERCADO_PAGO_ACCESS_TOKEN, SENDGRID_API_KEY) are configured in Netlify UI
  # Non-secret environment variables:
  SENDGRID_FROM_EMAIL = "noreply@acp-asociados.com"
  ADVISOR_EMAIL = "asesor.pac@gmail.com"
  SITE_URL = "https://acp-asociados.netlify.app"
  NODE_ENV = "production"
  PRICE_BASIC_CLP = "1000"
  PRICE_PREMIUM_CLP = "11000"
  # Flow Payment Gateway Credentials (Sandbox)
  # IMPORTANT: Using original registered credentials from Flow account
  FLOW_API_KEY = "7407DEBF-783B-4C84-9FB4-43C4L344D745"
  FLOW_SECRET_KEY = "419fd1dc315b285498f60189ae50507c1df2dd6a"
